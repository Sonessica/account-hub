services:
  openbento-review:
    build: .
    container_name: account-hub
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: review-placeholder
      NEXT_PUBLIC_APP_URL: https://account.atchooo.com:2096
    networks:
      - nas-frontend

networks:
  nas-frontend:
    external: true
