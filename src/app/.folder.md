# Folder: /src/app

1. **Status**: Next.js App Router 页面与 API。
2. **Logic**: `/`、`/editor` 跳转至 `/bento/editor`；当前页面经 `/api/private/editor` 读写 SQLite，经 `/api/private/media` 上传图片。
3. **Constraints**: `/auth/*`、`/api/auth/*`、`/[username]` 等旧版多用户路由不构成当前编辑器的密码门禁。

`/bento`、`/bento/widgets`、`/showcase` 仍是演示页面；`layout.tsx` 与 `globals.css` 为全局外壳和样式。
