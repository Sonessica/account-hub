# Folder: /src

1. **Status**: Next.js 应用源码；当前单页编辑器与部分 OpenBento 旧版流程并存。
2. **Logic**: `app/` 提供路由，`bento/` 提供卡片和画布，`lib/server/` 提供 SQLite 持久化，`design-system/` 提供通用 UI。
3. **Constraints**: 区分当前 `/bento/editor` 与旧版 Supabase 多用户路径；架构描述以实际调用链为准。

主要目录：`app/`、`bento/`、`components/`、`design-system/`、`lib/`、`stores/`、`types/`。
