# Folder: /src/bento/grid

1. **Status**: 保留的固定网格布局模块，不是当前无限画布的主要定位算法。
2. **Logic**: `BentoGrid.tsx`、`BentoCell.tsx`、`useGridLayout.ts` 与 `compactGridLayout.ts` 为旧版或演示场景排列卡片。
3. **Constraints**: 调整固定网格前检查调用页面；当前 `/bento/editor` 的碰撞与排位在 `editor/InfiniteCanvas.tsx`。
