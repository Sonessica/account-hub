# Folder: /src/bento/dnd

1. **Status**: 保留的 `@dnd-kit` 固定网格拖放模块。
2. **Logic**: `GridDndProvider.tsx` 为旧版网格提供拖放；当前无限画布的平移、拖动、交换与推挤在 `editor/InfiniteCanvas.tsx`。
3. **Constraints**: 修改当前编辑器拖动行为时先定位 `InfiniteCanvas`，不要误改本模块。
