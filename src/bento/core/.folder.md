# Folder: /src/bento/core

1. **Status**: Bento 卡片的基础组件与尺寸系统。
2. **Logic**: `BentoCard.tsx` 与 `BentoSlots.tsx` 组合卡片内容，`BentoSizeMap.ts` 定义尺寸/间距，`BentoContext.tsx` 与 `useBentoCard.ts` 支持卡片交互。
3. **Constraints**: 调整尺寸时同时检查无限画布的占格与像素尺寸计算，避免显示尺寸和碰撞范围不一致。
