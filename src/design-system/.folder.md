# Folder: /src/design-system

1. **Status**: 通用设计 token、主题、基础组件与组合组件。
2. **Logic**: `tokens/` 提供值，`foundation/` 提供主题，`primitives/` 和 `patterns/` 组合 UI，`utils/` 提供样式工具。
3. **Constraints**: 新增通用组件时优先复用 token 与主题；现有页面仍含直接样式值，不能假定全部组件已统一到 token。
