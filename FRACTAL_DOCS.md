# 代码地图维护约定

仓库保留了 OpenBento 时期的分层文档习惯：根目录 `README.md` 说明当前产品与部署，各主要源码目录的 `.folder.md` 说明本目录职责，部分文件头注释说明输入、输出与位置。`.cursor/rules/fractal.mdc` 是对应的编辑器规则。

维护原则：

1. 修改功能或调用链时，先以实际代码为准，再更新直接相关的 `.folder.md`；影响产品入口、部署或数据模型时同步更新 `README.md`。
2. 文件头注释若描述了已变化的行为，应随代码更新；不要为了遵循旧模板而写入与实现不符的说明。
3. 当前单页编辑器与保留的 Supabase 多用户代码并存。局部代码地图要明确它们的边界，不把历史设计当作现行架构。
4. 删除功能时清理失效文档与链接；阶段性比较报告不再放在当前产品文档树中，历史内容可从 Git 历史查看。

`.folder.md` 是快速导航，不替代类型定义、测试或运行中的代码。目录清单不必罗列每个文件，但关键职责和数据流必须准确。
